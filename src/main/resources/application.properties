# ? APP NAME
spring.application.name=airBnbApp

# ===============================
# ? DATABASE (PostgreSQL)
# ===============================
spring.datasource.url=jdbc:postgresql://localhost:5432/airBnb
spring.datasource.username=postgres
spring.datasource.password=bhumi2605

spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# ? JPA / HIBERNATE
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# ? SERVER
# ===============================
server.port=8080

# ? OPTIONAL (only if you want cleaner URLs)
# If you ENABLE this ? change controller mappings accordingly
# server.servlet.context-path=/api/v1

# ===============================
# ? LOGGING (DEBUG)
# ===============================
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE
logging.level.org.springframework=INFO

# ===============================
# ? JWT CONFIG
# ===============================
jwt.secretKey=bchbyw87748fjnrthv8h5uguh8hv9333unu58h5h44884bvubru48h8vscgyvtsbcy

# ===============================
# ? FRONTEND URL (VERY IMPORTANT)
# ===============================
frontend.url=http://localhost:5173

# ===============================
# ? RAZORPAY CONFIG
# ===============================
razorpay.key=rzp_test_SUL7XstcBKfgjN
razorpay.secret=CXnDk0YcYFO6to39Mu3OVMha

# Webhook secret (optional)
razorpay.webhook.secret=whsec_test_1234567890abcdef